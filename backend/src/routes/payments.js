const express = require('express');
const router = express.Router();

// Placeholder para rutas de pagos
router.get('/', (req, res) => {
  res.json({ message: 'Payments endpoint - En desarrollo' });
});

module.exports = router;